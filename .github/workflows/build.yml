name: Build
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Print message
        run: echo "build 🛠️"

      - name: Deploy
        uses: ./.github/workflows/deploy.yml
