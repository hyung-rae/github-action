name: caller-workflows

on:
  push:
    branches: [main]

jobs:
  caller-workflow:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Call workflow
        uses: ./called-workflows.yml
        with:
          caller-path: ${{ github.event_path}}
